-- -----------------------------------------------------
-- Custom animations (disabled)
-- -----------------------------------------------------

hl.config({
    animations = {
        enabled = false,
    },
})